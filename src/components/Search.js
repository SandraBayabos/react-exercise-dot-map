import React, { useRef } from "react";

const Search = () => {
  return (
    <div>
      <header>
        <form>
          <input type="text" placeholder="Type to filter..."></input>
        </form>
      </header>
    </div>
  );
};

export default Search;
